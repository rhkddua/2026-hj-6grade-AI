# 새 세션 첫 지시문

아래 내용을 새 세션의 첫 메시지로 그대로 사용하세요.

---

작업 루트는 `C:\Users\rhkdd\OneDrive\문서\2학기 전학공\web`입니다.

먼저 루트의 `SESSION_HANDOFF.md`를 처음부터 끝까지 읽고, `web/AGENTS.md`, 현재 설치된 `sites:sites-building` 및 `sites:sites-hosting` 지침을 읽으세요. 그 다음 상위 저장소와 `web` 내부 저장소의 `git status --short`를 각각 확인하세요. 기존 변경은 reset, checkout, clean으로 삭제하지 말고 강제 push도 하지 마세요.

이번 세션의 목표는 AI 코딩 교실 웹사이트 전체 회귀 점검과 발견 문제 수정입니다. 새 기능을 먼저 추가하지 말고 현재 1~10차시, 학생 인증·저장, 최고관리자 인증·대시보드, 공개 Sites 배포가 서로 일관되게 작동하는지 확인하세요.

반드시 점검할 항목:

1. 미로그인 상태에서 `/`, `/lesson/1`, `/lesson/8`, `/lesson/9`, `/lesson/10`, `/admin`의 보호와 이동 경로
2. 학생 로그인 후 홈에서 1~10차시가 모두 열리는지, 관리자 화면 접근이 차단되는지
3. 8·9·10차시에서 단계 이동, 선택·작성 내용의 800ms 자동 저장, 저장 큐, 실패 재시도, 새로고침 복원
4. 각 차시의 오답·미완료 상태 완료 차단, 완료 저장, 완료 후 내용 수정 시 `completed=false` 해제
5. `lesson_progress`가 새 테이블 없이 정확한 `lesson_no`와 `activity_data`를 사용하는지
6. 최고관리자 전용 로그인, 학생 로그인과의 양방향 전환, 비관리자 접근 차단
7. 관리자 대시보드가 1~10차시를 집계하고 학생 상세, 검색·필터, 완료율, CSV에 최신 기록을 반영하는지
8. 데스크톱과 모바일 폭, 키보드 조작, 상태·오류 문구와 개인정보 비요구 여부
9. 관련 소스 lint와 `npm run build`
10. 실제 공개 URL에서 로그인·저장·새로고침 복원·권한 분리 확인

테스트 계정은 루트 `TEST_ACCOUNT.md`에서 확인하되 비밀번호를 코드, 로그, 일반 문서나 답변에 복사하지 마세요. 테스트가 끝나면 학생과 관리자 세션을 로그아웃하세요.

Sites 작업 시 기존 프로젝트 ID `appgprj_6a962dcd21f08191876edad89331f7c3`와 `public` 접근 범위를 유지하고 새 Site를 만들지 마세요. 수정이 필요한 경우에만 새 source write credential을 발급받아 `credential.interactive=never`와 일회성 Bearer 헤더로 push하세요. 원격 이력 충돌은 fetch·병합으로 해결하고 강제 push하지 마세요. 배포 성공 후 공개 URL을 확인하세요.

`web/.git`, `.site-stage-*`, `site-*.tar.gz`는 임시 배포 산출물입니다. 삭제가 필요하면 먼저 정확한 절대 경로와 상위 저장소 경계를 검증하고, 사용자 변경을 포함하지 않는지 확인하세요. 작업 마지막에는 `SESSION_HANDOFF.md`를 갱신하고 상위 저장소의 최종 `git status --short`를 보고하세요.

점검 중 발견한 문제는 재현 근거와 영향 범위를 먼저 확인한 뒤 수정하고, 수정한 항목은 실제 동작과 빌드로 다시 검증하세요.

---
