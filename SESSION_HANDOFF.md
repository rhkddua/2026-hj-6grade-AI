# AI 코딩 교실 — 현재 작업 인수인계

> 최종 갱신: 2026-09-23 (KST)  
> 다음 단계: **1~10차시를 한 차시씩 검토하며 내용·활동을 추가, 수정, 삭제**

## 1. 다음 세션의 최소 컨텍스트

기본으로 아래 세 파일만 읽는다.

1. `web/AGENTS.md`
2. 이 문서
3. `LESSON_EDITING_GUIDE.md`

`PRD.md`, `README.md`, `LESSON_2_STATUS.md`, 과거 Git 이력은 현재 요청을 이해하는 데 필요할 때만 읽는다. 다음 세션에서는 전체 차시를 한꺼번에 분석하지 말고 사용자가 지정한 차시 파일만 연다.

작업 전에는 상위 저장소와 `web` 내부 저장소의 `git status --short`를 확인한다. 기존 변경을 reset, checkout, clean으로 제거하지 않는다.

## 2. 현재 제품 상태

- 초등학교 6학년 대상 AI 코딩 수업 웹사이트다.
- 학생 홈과 1~10차시가 모두 구현되어 있다.
- 학생 이메일·비밀번호 로그인, Supabase 진도 저장·복원, 최고관리자 로그인과 대시보드가 동작한다.
- 관리자 대시보드는 1~10차시 집계, 학생 검색·필터·상세, 완료율과 CSV를 제공한다.
- 공개 사이트: <https://hj-ai-coding-class-2026.rhkdduavud.chatgpt.site>

## 3. 배포 기준 상태

- Sites 프로젝트 ID: `appgprj_6a962dcd21f08191876edad89331f7c3`
- 접근 범위: `public`
- 최신 배포: **version 31**, `active`
- Sites 소스 커밋: `59b03d54e6cf896a32637aad83e865d5fb906151`
- version 31에서 1차시 관련 소스 lint와 `npm run build`가 성공했다.
- 공개 URL에서 JavaScript 빈칸 조립, 언어 비교 팝업, `Executing...` 실행 애니메이션, 누적 콘솔 로그, 자동 저장·새로고침 복원·완료 차단/저장/해제를 확인했다.
- 학생과 관리자 테스트 세션은 로그아웃 상태로 마쳤다.

## 4. 반드시 유지할 동작

- 모든 차시는 `useStudentSession()`으로 보호한다.
- 진도는 기존 `lesson_progress`의 `lesson_no`와 `activity_data jsonb`를 사용한다. 차시별 새 테이블을 만들지 않는다.
- 선택·작성 내용은 약 800ms 후 자동 저장한다.
- 저장 큐, 늦은 응답 보호, 실패 재시도, 새로고침 복원을 유지한다.
- 로드 실패 시 빈 상태로 기존 기록을 덮지 않는다.
- 오답·미완료 상태에서는 완료할 수 없다.
- 완료 후 답을 수정하면 `completed = false`로 되돌린다.
- 실제 학생 개인정보를 묻거나 예시 데이터로 넣지 않는다.
- 모바일과 키보드 조작을 유지한다.

## 5. 핵심 파일

- 학생 홈: `web/app/page.tsx`
- 차시 UI: `web/app/lesson/<번호>/page.tsx`
- 2~10차시 활동 UI: `web/app/lesson/<번호>/content.tsx`
- 차시 데이터·검증: `web/lib/lesson-*.ts`
- 공통 진도 저장: `web/lib/lesson-progress.ts`
- 학생 인증: `web/lib/student-auth.ts`
- 관리자 집계: `web/lib/admin-dashboard.ts`
- Supabase 스키마: `web/supabase/schema.sql`

차시별 정확한 파일 대응과 검증 범위는 `LESSON_EDITING_GUIDE.md`를 따른다.

## 6. 최근 수정 사항

- JavaScript 계산 미션을 `console.log( [숫자] + [숫자] );` 빈칸 조립 방식으로 바꾸고, `console.log` 스피커 비유, 기계어·어셈블리어 비교 팝업, 개발자 콘솔 디자인, 0.5초 실행 애니메이션과 `> 결과` 누적 로그를 추가했다. 기존 `mathInputs[2]`의 완성된 문자열 저장 형식을 유지해 과거 기록과 호환된다.
- 1차시 3단계를 ‘게임·수학 코딩 실험실’로 바꾸고, 기계어·어셈블리어·JavaScript를 직접 입력해 `3 + 2`를 계산하는 3개 미션을 추가했다. 수업용 기계어 약속, 입력 조각, 오답 피드백을 제공한다.
- 이동 활동과 계산 활동을 상단의 두 선택 카드와 독립된 패널로 분리해 한 번에 하나만 보이게 했다. 각 카드에는 완료 수·완료 배지와 활동 간 이동 버튼을 제공한다.
- 어셈블리어 계산을 `MOV A, #3` → `ADD A, #2` → `OUT A`로 바꾸고, 계산 방 `[ A ]`가 비어 있음 → 3 → 5로 변한 뒤 화면에 5를 출력하는 시각 피드백과 실행 애니메이션을 추가했다. 과거 `LOAD 3 / ADD 2 / PRINT` 기록은 새 형식으로 자동 변환한다.
- 기계어 계산 힌트에 `0001 = 불러오기`, `0010 = 더하기`, `1111 = 결과 보여주기` 명령 정보와 `3 = 0011`, `2 = 0010` 숫자 정보를 분리해 제공한다. 명령과 숫자를 결합한 정답 문장은 표시하지 않는다.
- 계산 입력·확인·성공 상태를 기존 `lesson_progress.activity_data`에 선택 필드로 저장하고, 계산 3개 완료를 1차시 완료 조건에 추가했다. 과거 기록은 기본값으로 복원되며 새 활동 완료 전에는 `completed=false`로 보호한다.
- 기존 네 언어 이동 게임과 800ms 자동 저장·저장 큐·오래된 응답 방지·재시도·로드 실패 보호·완료 후 수정 시 완료 해제를 유지했다.
- version 24 배포용 `.site-stage-63076ba`와 `site-63076ba7ab84b64ba5ffe4db4dc603cc160ae9fc.tar.gz`, 이전 세션의 다른 staging/archive와 중첩 `web/.git`은 사용자 변경 보존을 위해 그대로 두었다.

## 7. 계정과 비밀정보

- 테스트 계정은 루트 `TEST_ACCOUNT.md`에서만 읽는다.
- 비밀번호, 키, source credential, 토큰을 코드·로그·일반 문서·답변에 복사하지 않는다.
- `.env.local`의 실제 값을 출력하거나 커밋하지 않는다.
- 테스트가 끝나면 학생과 관리자 모두 로그아웃한다.

## 8. 다음 단계의 작업 원칙

- 한 세션에서는 가능하면 한 차시만 다룬다.
- 편집 전에 해당 차시의 현재 단계·활동·완료 조건을 짧게 정리한다.
- 사용자의 요구를 `유지 / 수정 / 추가 / 삭제`로 나눠 반영한다.
- 기존 저장 데이터와 호환되지 않는 변경은 영향과 마이그레이션 필요성을 먼저 알린다.
- 공통 저장·인증 코드를 건드리지 않았다면 전체 10차시 회귀 테스트를 반복하지 않는다.
- 변경 범위에 맞는 lint, build, 해당 차시 E2E를 수행하고 배포 후 공개 URL에서 한 번 확인한다.
- 이 문서에는 현재 상태와 최근 변경만 유지한다. 긴 작업 일지는 추가하지 않고 Git 이력을 사용한다.
